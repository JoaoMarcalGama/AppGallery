<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AppGallery.XamarinForms.Listas.CarrosselControle.Carrossel">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="CarouselView"/>
            <Label Text="Propriedade(s): ItemSource, ItemTEeplate"/>
            <Label Text="Evento(s): "/>

            <CarouselView x:Name="Carrossel01">
                <CarouselView.ItemTemplate>
                    <DataTemplate>
                        <Frame>
                            <StackLayout>
                                <Label Text="{Binding Nome}" TextColor="Black" FontAttributes="Bold"/>
                                <Label Text="{Binding Cargo}" TextColor="Gray" FontSize="12"/>
                                <Label Text="{Binding Descricao}" TextColor="Black" FontSize="18"/>
                            </StackLayout>
                        </Frame>
                    </DataTemplate>
                </CarouselView.ItemTemplate>
            </CarouselView>
            
        </StackLayout>
    </ContentPage.Content>
</ContentPage>